services:
  - type: web
    name: nse-option-signal-assistant
    runtime: python
    buildCommand: pip install uvicorn fastapi
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port 10000
